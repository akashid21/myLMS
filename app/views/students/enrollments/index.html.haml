%h1 Enrolled courses
%table.table
  %thead
    %tr
      %th Name
      %th Description
  %tbody
    - @enrollments.each do |enrollment|
      %tr
        %td= enrollment.course.name
        %td= enrollment.course.description
